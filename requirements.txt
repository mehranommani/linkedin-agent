# LinkedIn AI Content Agent - Root Dependencies
# ==============================================
# For quick local dev: pip install -r requirements.txt
# For Docker: see backend/requirements.txt

# Backend (FastAPI + MCP + LangGraph)
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database (SQLite is stdlib — no install needed)
# duckdb is only needed to run the one-time migration script

# MCP
mcp>=1.0.0

# LangGraph Pipeline
langgraph>=0.2.0
langchain-core>=0.2.0

# HTTP
httpx>=0.27.0
requests>=2.31.0

# Content Fetching
feedparser>=6.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
html2text>=2024.2.26

# Data Processing
pandas>=2.0.0

# DSPy — prompt optimization + runtime Refine retries
dspy-ai>=2.6

# Optional: Evaluation (not required for core pipeline)
# ragas>=0.1.0
# langsmith>=0.1.0
# datasets>=2.14.0
